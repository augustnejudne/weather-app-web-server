<!DOCTYPE html>
<html lang="en">
{{>head}}
<body>
  <div class="main-content">
    {{>header}}
    <h4>{{name}} is a {{job}} who is {{dream}}</h4>
    <img src="img/cat.jpeg" alt="">
  </div>
  {{>footer}}
</body>
</html>
